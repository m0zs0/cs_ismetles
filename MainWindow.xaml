<Window x:Class="ism_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ism_wpf"
        mc:Ignorable="d"
        
Title="User Management" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- DataGrid -->
        <DataGrid x:Name="dgUsers" Grid.Row="1" AutoGenerateColumns="False" 
                  SelectionMode="Single" SelectionUnit="FullRow" 
                  IsReadOnly="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Név" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Jelszó" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Regisztráció" Binding="{Binding RegistrationDate}" />
                <DataGridTextColumn Header="Szint" Binding="{Binding Level}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Gombok -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Új felhasználó" Click="BtnAdd_Click" Margin="5"/>
            <Button Content="Törlés" Click="BtnDelete_Click" Margin="5"/>
            <Button Content="Mentés CSV-be" Click="BtnSave_Click" Margin="5"/>
            <Button Content="Betöltés CSV-ből" Click="BtnLoad_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>

